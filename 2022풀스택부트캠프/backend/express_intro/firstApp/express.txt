send() 메서드는 Http 요청을 받아와서
한 페이지에 하나만 사용해야한다. 여러개 사용할수 없다.


get()메서드
get('주소', (request, response) => {
  // 코드
})


정해진 경로를 제외한 나머지 경로 메세지 *
쓰려면 코드 가장 마지막에 써야한다. 맨 하단.
app.get('*', (req, res) => {
  res.send('메세지');
});